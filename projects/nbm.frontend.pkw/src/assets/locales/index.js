import flatObject from '../../utils/flat-object'

import zh from './zh'
import en from './en'
import vi from './vi'

export default {
  zh: flatObject(zh),
  en: flatObject(en),
  vi: flatObject(vi),
}
